import { LegalFounderLeadership } from "@/components/pages/legal-founder-leadership";

/** Legal → Team & Roles (`/legal?view=careers`). Renders {@link LegalFounderLeadership}. */
export default function CareersPage() {
  return <LegalFounderLeadership />;
}
